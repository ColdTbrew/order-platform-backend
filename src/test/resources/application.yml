spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: "password"
  ai:
    openai:
      enabled: false
      api-key: test-api-key

redis:
  refund-queue: refund-queue-test
